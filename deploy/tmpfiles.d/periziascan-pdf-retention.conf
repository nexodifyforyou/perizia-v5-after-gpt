# Root owns the dedicated root. The backend group has write+traverse only (no
# directory read/list permission); ciphertext children are created 0600.
d /srv/perizia/private/pdf_retention 0730 root syedtajmeelshah -
